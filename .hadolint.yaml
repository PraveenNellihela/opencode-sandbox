---
ignored:
  - DL3008  # Pin versions in apt get install: too strict for dev containers
  - DL3059  # Multiple consecutive RUN: acceptable for conditional installs
  - DL4006  # Set SHELL option -o pipefail: already set globally in Dockerfile
trustedRegistries:
  - docker.io
  - deb.nodesource.com
